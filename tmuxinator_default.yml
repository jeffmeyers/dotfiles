name: default
root: ~/working

windows:
  - shell:
  - code:
      - vim
  - notes:
      root: ~/Dropbox/notes
      panes:
        - vim
  - server:
      layout: main-vertical
      panes:
        - dev/up
        - bin/foreman s
